# Rubric

- `dig(depth)` returns a clear burrow description for the given depth.
- `Burrow` accumulates depth across repeated `dig` calls via `total`.
- `tests/test_wombat.py` exists and the full suite passes.
