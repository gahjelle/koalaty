Write a Python module `quokka.py` that defines a function `greet()` taking no
arguments and returning the string `"Quokka!"`.

Keep the module importable and the function signature exactly as described.
